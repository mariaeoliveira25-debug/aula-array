  // Exemplo de array 
  
  let sucos = ("maçã", "banana", "laranja", "uva");

  // Criando um array de números 
  let numeros = [1, 2, 3, 4, 5];
  console.log(numeros[2]);
  
   // Acessando um elemento especifico do array 
   let animais = ["cachorro", "Gatos", "Passaro", "Peixe"];
   console.log(animais[1]);

    //Alterando um elemneto do array
    let times = ["Flamengo","Palemiras", "São Paulo ", "Corinthians"];
    times[2]= "Vasco";
    console.log(times);
    
    // Descobrindo o tamanho do array 
    console.log(games.length);

    // Adicionando um elemento ao final do array
    let cores = ["Vermelho","Azul", "Verde"];

    // Removendo o último elemento do array
    let carros = [ "Fusca", "Gol", "Civic", "Corolla"];
    carros.pop();
    console.log(carros);

    // Removendo o primeiro elemento do array
    let cidades = ["São Paulo", "Rio d janeiro", "Belo Horizonte", "Curitiba "];
    cidades.shift();
    console.log (cidades);

    //Adicionando um elemento no inicio do array 
    let países = ["Brasil", "Argentina", "Chile"];
    console.log(países);